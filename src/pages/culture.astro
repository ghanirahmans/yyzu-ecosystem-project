---
import CtaBand from "../components/CtaBand.astro";
import InfoCard from "../components/InfoCard.astro";
import PageHero from "../components/PageHero.astro";
import Section from "../components/Section.astro";
import { fitPeople, philosophy, values } from "../data/site";
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Culture & Engineering Mindset | YYZU"
  description="Engineering culture, collaboration mindset, ownership, discipline, contribution, dan growth culture yang menjadi fondasi YYZU."
>
  <PageHero
    eyebrow="Culture & Engineering Mindset"
    title="Kultur YYZU membentuk cara belajar, bekerja, dan berkontribusi."
    description="YYZU ingin menjadi lingkungan yang serius tetapi tetap welcoming: tempat belajar, berdiskusi, membangun, menerima feedback, melatih ownership, dan mengembangkan engineering mindset secara bertahap."
    note="Fokusnya culture over hype, contribution over passive membership, consistency over popularity."
    tone="dark"
  />

  <section class="bg-white py-16 sm:py-20">
    <div class="mx-auto max-w-7xl px-5 sm:px-6 lg:px-8">
      <Section
        eyebrow="Core Values"
        title="Build Together, Industry-Oriented, Continuous Growth, Real Contribution."
        description="Nilai ini bukan slogan. Nilai ini dipakai untuk menentukan aktivitas, cara join, cara bekerja, dan cara menilai progress."
        align="center"
      />
      <div class="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">
        {values.map((item) => <InfoCard {...item} />)}
      </div>
    </div>
  </section>

  <section class="bg-[#f7f9fb] py-16 sm:py-20">
    <div class="mx-auto grid max-w-7xl gap-10 px-5 sm:px-6 lg:grid-cols-[0.9fr_1.1fr] lg:px-8">
      <Section
        eyebrow="Principles"
        title="Kolaborasi di atas kompetisi, kontribusi di atas membership pasif."
        description="YYZU memilih proses yang sehat: konsistensi, ownership, review, dokumentasi, dan growth yang bisa dilihat lewat kontribusi nyata."
      />
      <div class="grid gap-4 sm:grid-cols-2">
        {
          philosophy.map((item) => (
            <article class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
              <h3 class="text-lg font-bold text-slate-950">{item.title}</h3>
              <p class="mt-3 text-sm leading-7 text-slate-600">
                {item.description}
              </p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="bg-slate-950 py-16 text-white sm:py-20">
    <div class="mx-auto max-w-7xl px-5 sm:px-6 lg:px-8">
      <Section
        eyebrow="Fit"
        title="YYZU tidak harus cocok untuk semua orang."
        description="YYZU cocok untuk orang yang ingin belajar serius, membangun bersama, menerima feedback, dan tumbuh lewat proses engineering yang praktis."
        align="center"
        tone="dark"
      />
      <div class="mt-12 grid gap-5 lg:grid-cols-2">
        {
          fitPeople.map((item) => (
            <article class="rounded-lg border border-white/12 bg-white/[0.06] p-7">
              <h3 class="text-xl font-bold text-white">{item.title}</h3>
              <p class="mt-4 text-sm leading-7 text-slate-300">
                {item.description}
              </p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <CtaBand
    title="Culture YYZU hidup lewat cara kita belajar, build, review, dan contribute."
    description="Kalau kamu ingin masuk ke ruang yang menuntut growth, ownership, dan kolaborasi, baca ekspektasi join sebelum menghubungi kami."
    primaryLabel="Baca Ekspektasi Join"
    primaryHref="/join/"
  />
</BaseLayout>
